# Add project specific ProGuard rules here.
# No rules are required yet — this project has no obfuscation-sensitive code.
#
# isMinifyEnabled is currently false, so this file is not exercised by R8.
# Before enabling minification, add and verify keep rules for reflection-based
# dependencies already on the classpath: Koin (viewModel { } DI), Room, Retrofit,
# and kotlinx.serialization — each has known obfuscation/reflection sensitivities.
